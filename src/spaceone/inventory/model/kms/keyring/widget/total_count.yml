---
cloud_service_group: KMS
cloud_service_type: KeyRing
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    options:
      default: 0
