---
cloud_service_group: AppEngine
cloud_service_type: Instance
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    options:
      default: 0
