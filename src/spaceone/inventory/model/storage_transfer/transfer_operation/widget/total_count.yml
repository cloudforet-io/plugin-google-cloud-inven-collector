---
cloud_service_group: StorageTransfer
cloud_service_type: TransferOperation
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    options:
      default: 0 