---
cloud_service_group: BigQuery
cloud_service_type: SQLWorkspace
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    options:
      default: 0