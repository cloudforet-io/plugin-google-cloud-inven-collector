---
cloud_service_group: CloudSQL
cloud_service_type: CloudSQL
name: Total Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    options:
      default: 0